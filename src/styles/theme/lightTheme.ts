import { theme } from "antd";

const lightTheme = {
  algorithm: theme.compactAlgorithm,
  token: {
    colorPrimary: "#52c41a",
  },
  components: {
    Layout: {
      headerBg: "white",
      bodyBg: "white",
      algorithm: true,
    },
  },
};

export default lightTheme;
